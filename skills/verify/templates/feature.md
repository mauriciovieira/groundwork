# <Feature name>

<!-- One sentence: what this lets someone do. -->

## What exists today

<!-- The behaviour as it actually is, not as intended. Note anything inferred rather than
     confirmed by running it. -->

## How a person gets here

<!-- The path a user takes: entry point, the steps, any state they need first (signed in, a
     record already created, a flag enabled). -->

## How an agent drives it

<!-- The exact commands. Include the preconditions and the expected observable outcome - the
     thing that proves it worked, not just that it ran without error. -->

```
./control drive <slug>
./control prove <slug>
```

Observable outcome: <!-- what specifically must be true afterwards -->

## Known failure modes

<!-- What breaks, what it looks like when it breaks, and anything that fails intermittently.
     An empty section here usually means nobody has driven this hard yet - say that rather
     than leaving it blank. -->
